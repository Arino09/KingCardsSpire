---
description: C# 必须显式写出访问修饰符（方法、字段、属性等）
globs: "**/*.cs"
alwaysApply: true
---

# C# 显式访问修饰符

编写或修改本仓库中的 C# 代码时，必须遵守：

- **所有方法**（含构造函数、静态方法、本地函数以外的成员方法、`override`/`abstract` 成员）均需显式写出访问修饰符：`public`、`private`、`protected`、`internal`、`protected internal`、`private protected` 之一。
- **所有字段与成员变量**（含实例字段、静态字段、`const`、`readonly`）均需显式写出访问修饰符。
- **所有属性**（含自动属性、索引器若适用同一约定）均需显式写出访问修饰符。
- **禁止**依赖 C# 默认可访问性（例如类型成员默认为 `private`、顶层类型默认为 `internal` 等仍建议在类型声明上对非嵌套类型使用显式 `internal`/`public`，嵌套类型同样显式写出）。

新建代码一律遵守；改动既有文件时，若触及相关成员，应顺手补全显式修饰符以保持风格一致。